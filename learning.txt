  // for uploading NFT to IPFS
    const ipfsUploadData = async () => {
      const ipfsData = await ipfsUploadMutation(uploadedImages[0]);
      setIpfsUrl(`${`https://${INFURA_URL}`}/ipfs/${ipfsData.path}`);
    };